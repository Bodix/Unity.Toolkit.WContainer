# WContainer

My personal improvements for [VContainer](https://github.com/hadashiA/VContainer) package. It inculdes:

- Advanced lifetime scope (`AdvancedLifetimeScope`)
  - Automatic injection to components.
- Optional dependencies (`builder.RegisterInstanceOptional`).
- `AutoInjector` for instantiated objects.